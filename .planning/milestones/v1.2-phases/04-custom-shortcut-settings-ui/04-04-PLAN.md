---
phase: 04-custom-shortcut-settings-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - CRNA-05
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Settings form opens and shows the 2x2 corner grid with labeled corners matching screen layout"
    - "Monitor selector is present and functional when more than one monitor is connected"
    - "Recording a custom shortcut (e.g. Ctrl+F5) shows the display text in the corner cell"
    - "Pressing Escape during recording cancels without dismissing the Settings form"
    - "Saving a corner configured with a custom shortcut and then dwelling in that corner fires the expected keys"
    - "Saving preserves all other settings (ZoneSize, DwellDelay, StartWithWindows)"
---

<objective>
Verify the complete Phase 4 feature end-to-end: the redesigned Settings UI, keystroke recording, and custom shortcut dispatch all work correctly from the user's perspective.

Purpose: Automated build verification catches compilation errors but not UI layout, recording UX, or the actual dispatch of recorded shortcuts. This checkpoint catches interaction bugs before the phase is marked complete.

Output: User confirms Phase 4 requirements are met (CRNA-05, UI-01, UI-02)
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release and run smoke check</name>
  <files></files>
  <action>
Build the release binary and verify the app starts cleanly:

```bash
cd /c/src/WindowsHotSpot
dotnet publish WindowsHotSpot/WindowsHotSpot.csproj -c Release -r win-x64 --self-contained false -o out/smoke
```

Then verify the output exists:
```bash
ls out/smoke/WindowsHotSpot.exe
```

If the build fails, fix compilation errors before proceeding to the checkpoint. The checkpoint must not be reached with a broken build.
  </action>
  <verify>
    <automated>cd /c/src/WindowsHotSpot && dotnet build WindowsHotSpot/WindowsHotSpot.csproj -c Release --no-restore 2>&1 | grep -E "^Build|error|warning" | head -20</automated>
    <manual>Confirm out/smoke/WindowsHotSpot.exe exists</manual>
  </verify>
  <done>dotnet publish succeeds with 0 errors. WindowsHotSpot.exe exists in out/smoke/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 4 Settings UI and custom shortcut dispatch end-to-end</name>
  <action>
Run out/smoke/WindowsHotSpot.exe (or dotnet run --project WindowsHotSpot/WindowsHotSpot.csproj), then test each scenario:

1. Right-click the tray icon and open Settings.
   EXPECTED: Form opens showing a 2x2 grid of corner controls (Top Left, Top Right, Bottom Left, Bottom Right) in the correct visual positions.

2. Check the action ComboBox in any corner cell.
   EXPECTED: Items include "Disabled", "Task View", "Show Desktop", "Action Center", "Custom Shortcut".

3. Select "Custom Shortcut" for one corner (e.g. Top Left).
   EXPECTED: A Record button and shortcut display area appear for that corner.

4. Click the Record button, then press Ctrl+F5.
   EXPECTED: Display area shows "Ctrl+F5" (or equivalent). Recording ends automatically.

5. Click Record again for the same corner, then press Escape.
   EXPECTED: Display reverts to previous label. Settings form stays open (NOT dismissed).

6. Monitor selector (if two monitors connected): a ComboBox appears at the top. Switching between monitors shows each monitor's independent corner assignments.
   If single monitor: confirm NO monitor selector is visible.

7. Click Save. Move the mouse to the corner configured in step 4.
   EXPECTED: Ctrl+F5 fires in the focused application.

8. Reopen Settings: confirm the recorded shortcut label still shows (persisted to settings.json).
  </action>
  <verify>
    <automated>MISSING — visual/functional verification only; no automated command can confirm UI layout or keystroke dispatch</automated>
    <manual>Walk through all 8 steps above and confirm each expected result matches actual behavior</manual>
  </verify>
  <done>All 8 verification steps pass. CRNA-05 (custom shortcut recorded, saved, dispatched), UI-01 (2x2 grid), UI-02 (monitor selector) confirmed working.</done>
</task>

</tasks>

<verification>
All Phase 4 success criteria verified by human checkpoint:
- CRNA-05: Custom shortcut recorded, saved, dispatched correctly
- UI-01: 2x2 corner grid matches physical screen layout
- UI-02: Monitor selector appears only with multiple monitors
</verification>

<success_criteria>
- Settings form visually matches the 2x2 physical corner layout
- Recording a shortcut persists across Settings open/close
- Escape during recording does NOT dismiss the form
- Custom shortcut fires correctly when mouse dwells in the configured corner
- Multi-monitor selector works (or is absent on single-monitor systems)
- Phase 4 requirements CRNA-05, UI-01, UI-02 confirmed by user
</success_criteria>

<output>
After completion, create `.planning/phases/04-custom-shortcut-settings-ui/04-04-SUMMARY.md`
</output>
