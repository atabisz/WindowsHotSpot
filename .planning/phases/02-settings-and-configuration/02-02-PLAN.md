---
phase: 02-settings-and-configuration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - WindowsHotSpot/UI/SettingsForm.cs
  - WindowsHotSpot/HotSpotApplicationContext.cs
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04]

must_haves:
  truths:
    - "User can open Settings from tray menu and see a dialog with corner dropdown, zone size input, dwell delay input, and Start with Windows checkbox"
    - "User can change the active corner via a dropdown and save"
    - "User can change zone size and dwell delay via numeric inputs and save"
    - "User can toggle Start with Windows and the registry key is created or removed"
    - "Clicking Save persists changes to settings.json and applies them live to detection"
    - "Clicking Cancel discards changes"
  artifacts:
    - path: "WindowsHotSpot/UI/SettingsForm.cs"
      provides: "Modal settings dialog with all 4 configurable options"
      contains: "class SettingsForm"
      min_lines: 60
    - path: "WindowsHotSpot/HotSpotApplicationContext.cs"
      provides: "OnSettingsClick wired to real SettingsForm (replaces placeholder)"
      contains: "SettingsForm"
  key_links:
    - from: "WindowsHotSpot/HotSpotApplicationContext.cs"
      to: "WindowsHotSpot/UI/SettingsForm.cs"
      via: "OnSettingsClick creates and shows SettingsForm modal"
      pattern: "new SettingsForm.*ShowDialog"
    - from: "WindowsHotSpot/HotSpotApplicationContext.cs"
      to: "WindowsHotSpot/Config/ConfigManager.cs"
      via: "OnSettingsClick reads form values into Settings and calls Save()"
      pattern: "_configManager\\.Save\\(\\)"
    - from: "WindowsHotSpot/HotSpotApplicationContext.cs"
      to: "WindowsHotSpot/Config/StartupManager.cs"
      via: "OnSettingsClick calls StartupManager.SetEnabled()"
      pattern: "StartupManager\\.SetEnabled"
---

<objective>
Build the Settings dialog and wire it into the tray menu, replacing the Phase 1 placeholder. User can configure corner, zone size, dwell delay, and Start with Windows through a modal WinForms dialog.

Purpose: Completes the user-facing configuration experience. After this plan, all Phase 2 success criteria are met -- settings are configurable, persisted, and take effect immediately.

Output: UI/SettingsForm.cs, updated OnSettingsClick in HotSpotApplicationContext.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-and-configuration/02-RESEARCH.md
@.planning/phases/02-settings-and-configuration/02-01-SUMMARY.md
@WindowsHotSpot/HotSpotApplicationContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsForm with manual layout</name>
  <files>
    WindowsHotSpot/UI/SettingsForm.cs
  </files>
  <action>
Create `WindowsHotSpot/UI/` directory and `SettingsForm.cs`:

- Internal sealed class `SettingsForm : Form` in namespace `WindowsHotSpot.UI`
- Add `using WindowsHotSpot.Config;` for AppSettings and HotCorner
- Set `AutoScaleMode = AutoScaleMode.Dpi` (prevents blurry controls on high-DPI, research Pitfall 5)
- Set `FormBorderStyle = FormBorderStyle.FixedDialog`, `MaximizeBox = false`, `MinimizeBox = false`, `StartPosition = FormStartPosition.CenterScreen`, `Text = "WindowsHotSpot Settings"`
- **NO designer file** -- all layout in code (manual layout, research recommends this for diffability)

**Controls (all private fields):**
1. `ComboBox _cornerCombo` -- DropDownStyle.DropDownList, DataSource = `Enum.GetValues<HotCorner>()`, SelectedItem from settings.Corner (SETT-01)
2. `NumericUpDown _zoneSizeInput` -- Minimum=1, Maximum=50, Increment=1, Value from settings.ZoneSize (SETT-02)
3. `NumericUpDown _dwellDelayInput` -- Minimum=50, Maximum=2000, Increment=50, Value from settings.DwellDelayMs (SETT-03)
4. `CheckBox _startupCheckBox` -- Text="Start with Windows", Checked from `StartupManager.IsEnabled` (read live from registry, NOT from settings file) (SETT-04)
5. `Button saveButton` -- Text="Save", DialogResult=DialogResult.OK
6. `Button cancelButton` -- Text="Cancel", DialogResult=DialogResult.Cancel

**Layout (manual positioning):**
- Labels on left column (~x:12), inputs on right column (~x:140)
- Row spacing ~33px: corner at y:15, zone at y:48, delay at y:81, startup checkbox at y:114, buttons at y:150
- ClientSize = new Size(310, 190)
- Set `AcceptButton = saveButton`, `CancelButton = cancelButton`

**Public read-only properties for retrieving values:**
- `HotCorner SelectedCorner => (HotCorner)_cornerCombo.SelectedItem!;`
- `int SelectedZoneSize => (int)_zoneSizeInput.Value;`
- `int SelectedDwellDelay => (int)_dwellDelayInput.Value;`
- `bool SelectedStartWithWindows => _startupCheckBox.Checked;`

**Constructor:** Takes `AppSettings settings` parameter, populates all controls from it. The startup checkbox reads from `StartupManager.IsEnabled` (not settings.StartWithWindows) to reflect actual registry state.
  </action>
  <verify>
    <automated>cd C:/src/WindowsHotSpot &amp;&amp; dotnet build WindowsHotSpot/WindowsHotSpot.csproj --no-incremental 2>&amp;1 | tail -5</automated>
    <manual>Verify 0 errors, 0 warnings</manual>
  </verify>
  <done>SettingsForm.cs exists in UI/ with all 4 input controls, Save/Cancel buttons, manual layout, AutoScaleMode.Dpi, and public properties for reading selected values. Builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SettingsForm into tray menu replacing placeholder</name>
  <files>
    WindowsHotSpot/HotSpotApplicationContext.cs
  </files>
  <action>
Modify `HotSpotApplicationContext.cs`:

- Add `using WindowsHotSpot.UI;`
- Replace the entire `OnSettingsClick` method body. Remove the placeholder MessageBox. New implementation:
  ```
  private void OnSettingsClick(object? sender, EventArgs e)
  {
      using var form = new SettingsForm(_configManager.Settings);
      if (form.ShowDialog() == DialogResult.OK)
      {
          _configManager.Settings.Corner = form.SelectedCorner;
          _configManager.Settings.ZoneSize = form.SelectedZoneSize;
          _configManager.Settings.DwellDelayMs = form.SelectedDwellDelay;
          _configManager.Settings.StartWithWindows = form.SelectedStartWithWindows;
          StartupManager.SetEnabled(form.SelectedStartWithWindows);
          _configManager.Save(); // Fires SettingsChanged -> CornerDetector.UpdateSettings
      }
  }
  ```
- This reads all 4 values from the form, writes them to AppSettings, updates the registry via StartupManager, and calls Save() which triggers SettingsChanged (wired in Plan 01 to call CornerDetector.UpdateSettings)
- The modal dialog is safe for the hook -- ShowDialog() runs its own nested message loop that dispatches WH_MOUSE_LL callbacks (research Pitfall 4 confirms this)
  </action>
  <verify>
    <automated>cd C:/src/WindowsHotSpot &amp;&amp; dotnet build WindowsHotSpot/WindowsHotSpot.csproj --no-incremental 2>&amp;1 | tail -5</automated>
    <manual>Verify 0 errors, 0 warnings. Verify OnSettingsClick no longer has MessageBox placeholder.</manual>
  </verify>
  <done>OnSettingsClick opens real SettingsForm, reads user choices, persists to settings.json and registry, and triggers live settings update to CornerDetector. No more placeholder MessageBox.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with 0 errors, 0 warnings
- UI/SettingsForm.cs exists with ComboBox for corner, NumericUpDown for zone and delay, CheckBox for startup
- HotSpotApplicationContext.OnSettingsClick creates SettingsForm, reads values on OK, calls ConfigManager.Save()
- The save path: form values -> AppSettings properties -> ConfigManager.Save() -> SettingsChanged event -> CornerDetector.UpdateSettings()
- StartupManager.SetEnabled called with checkbox value on save
</verification>

<success_criteria>
- SETT-01: SettingsForm has corner ComboBox populated with HotCorner enum values
- SETT-02: SettingsForm has zone size NumericUpDown (1-50 range)
- SETT-03: SettingsForm has dwell delay NumericUpDown (50-2000 range)
- SETT-04: SettingsForm has Start with Windows checkbox wired to StartupManager
- Full save flow: dialog -> AppSettings -> JSON file + registry + live CornerDetector update
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-and-configuration/02-02-SUMMARY.md`
</output>
